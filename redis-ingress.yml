apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: redis-cluster
  namespace: redis
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
    traefik.ingress.kubernetes.io/router.rule: "Host(`http://3.36.127.101`) && PathPrefix(`/redis`)"
spec:
  rules:
    - host: http://3.36.127.101
      http:
        paths:
          - path: /redis
            backend:
              serviceName: redis
              servicePort: 6379
